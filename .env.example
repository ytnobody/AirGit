# AirGit Configuration
# Copy this to .env or set these as environment variables

# SSH Connection Settings
AIRGIT_SSH_HOST=your-server.com
AIRGIT_SSH_PORT=22
AIRGIT_SSH_USER=git
AIRGIT_SSH_KEY=${HOME}/.ssh/id_rsa

# Git Repository Path (absolute path on remote server)
AIRGIT_REPO_PATH=/var/git/my-repo

# Server Settings
AIRGIT_LISTEN_ADDR=0.0.0.0
AIRGIT_LISTEN_PORT=8080
